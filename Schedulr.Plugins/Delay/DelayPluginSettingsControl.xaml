<UserControl x:Class="Schedulr.Plugins.Delay.DelayPluginSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Label Content="Delay for" VerticalAlignment="Center" />
            <TextBox Text="{Binding Path=DelaySeconds, Mode=TwoWay}" MinWidth="40" Margin="5,0,0,0" VerticalAlignment="Center" />
            <Label Content="seconds before running the next action." Margin="5,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
